#include <bits/stdc++.h>
#define rep(i,n) for(int i=0; i < (n); i++)
using namespace std;
typedef long long ll;
int main() {
    double a,b; cin>>a>>b;
    // double ans=100-(100*b)/a;
    cout << fixed << setprecision(10) << 100-(100*b)/a << endl;
}